#include "main.h"

/**
 * mul(int a, int b)- prints the multiplication of
 * two integers
 *
 * @a: first integer
 * @b: second integer
 *
 * Return: the result
 */

int mul(int a, int b)
{
	return (a * b);
}
